rules_version = '2';
service firebase.storage {
  match /b/{bucket}/o {
    match /{allPaths=**} {
      // Hər kəsə oxuma icazəsi (şəkilləri görmək üçün)
      allow read: if true;
      
      // Yalnız admin və moderatorlara yazma icazəsi
      allow write: if request.auth != null;
    }
  }
}